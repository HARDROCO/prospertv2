import 'package:flutter/material.dart';

class Pagechat extends StatelessWidget {
  const Pagechat({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text("Chat", style: TextStyle(fontSize: 30),)
    );
  }
}